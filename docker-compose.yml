services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    dns:
      - 8.8.8.8
      - 8.8.4.4
    depends_on:
      - redis

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
